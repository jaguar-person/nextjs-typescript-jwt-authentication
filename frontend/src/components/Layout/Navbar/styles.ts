import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  grow: {
    flexGrow: 1,
  },
  logo: {
    color: "#FFF",
    textDecoration: "none",
    width: 150,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
}));
